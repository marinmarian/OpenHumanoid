# Object Mesh Models

Place `.obj` or `.stl` mesh files here for FoundationPoseROS2 to detect.

Directory structure:
```
demo_data/
  cup/
    cup.obj
  bottle/
    bottle.stl
```

Each object gets its own subfolder with a mesh file.
FoundationPoseROS2 will present a GUI to select which objects to track.
